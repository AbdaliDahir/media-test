import styles from './styles.module.css'
import { Link } from 'react-router-dom'

const CrudOperation = ({
  item,
}) => {
  return (
    <div> 
      <Link to={`/add/product`}>
        
      </Link>  
    </div>
  )
}

export default CrudOperation
